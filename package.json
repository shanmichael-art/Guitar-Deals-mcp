{
  "name": "guitar-deals-mcp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^latest",
    "playwright": "^latest",
    "zod": "^latest"
  },
  "devDependencies": {
    "@types/node": "^latest",
    "tsx": "^latest",
    "typescript": "^latest"
  }
}